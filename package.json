{
  "name": "discord-bot",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"  // Railway 默认会执行此命令
  },
  "dependencies": {
    "discord.js": "^14.13.0",
    "dotenv": "^16.3.1"       // 用于本地测试，Railway 无需显式加载 .env
  }
}
